L'ensemble $H=\{x\in\mathbb R^n\mid a^\top x=b\}$ est appelé un hyperplan

L'hyperplan coupe toujours l'espace avec $n-1$ dimensions, donc en $\mathbb R^2$ l'hyperplan est une droite et en $\mathbb R^3$ un plan.

