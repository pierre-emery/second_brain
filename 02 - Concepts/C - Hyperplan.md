L'ensemble $H=\{x\in\mathbb R^n\mid a^\top x=b\}$ est appelé un hyperplan

L'hyperplan de $\mathbb R^n$ est un sous-espace de dimension $n-1$ donc en $\mathbb R^2$ l'hyperplan est une droite et en $\mathbb R^3$ un plan.

